version: '3.7'

services:
  app:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: node-app
    ports:
      - 3003:3003
    volumes:
      - ./:/home/app/
    networks:
      - app-network

  mysqldb:
    image: mysql:8.0.21
    container_name: mysql-db
    environment:
      MYSQL_ROOT_PASSWORD: 4321
    ports:
      - 3307:3306
    volumes:
      - colab-v2_volume_db:/var/lib/mysql
    networks:
      - app-network


networks:
  app-network:
    driver: bridge

volumes:
  colab-v2_volume_db: {}
